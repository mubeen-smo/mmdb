# retrieval.py — kept for backward compatibility but no longer used by the
# agentic pipeline. All embedding, vector search, scoring, and merging now
# live inside search_places / search_items in tools.py.
